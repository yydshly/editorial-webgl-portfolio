import { defineConfig, devices } from "@playwright/test";

// Runs focused About evidence against the separately started test server.
// Deliberately has no webServer entry so it never touches a user's dev preview.
export default defineConfig({
  testDir: "./tests/e2e",
  fullyParallel: false,
  retries: 0,
  reporter: "list",
  use: {
    baseURL: "http://127.0.0.1:3100",
    trace: "retain-on-failure",
  },
  projects: [
    {
      name: "chromium",
      use: { ...devices["Desktop Chrome"] },
    },
  ],
});
